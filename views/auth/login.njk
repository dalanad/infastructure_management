{% extends "_layout/admin-base.njk" %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-4 col-md-6 col-sm-8 ml-auto mr-auto">
                <form class="p-4 mx-auto" method="post" style="max-width: 300px;">
                    <img src="/img/logo-03.png" class="logo">
                    <h5 class="m-1 text-center">IT Asset Management</h5>
                    <h4 class="mb-4 text-center">Sign In</h4>
                    <button class="btn  w-100"  type="button" disabled>Sign in using SSO</button>
                    <div class="align-items-center mt-4 mb-3 d-flex flex-row"  >
                        <div style="background:var(--muted);height:1px;flex:1"></div>
                        <small class="mx-2">or using user id and password</small>
                        <div style="background:var(--muted);height:1px;flex:1"></div>
                    </div>
                    <div class="field">
                        <label for="uid">User ID</label>
                        <input type="text" id="uid" class="ctrl" name="uid" required="" autofocus="">
                    </div>
                    <div class="field ">
                        <label for="password" class="d-flex align-items-center justify-content-between">Password
                            <small>
                                <a class="float-end" tabindex="3" href="/auth/forgot/">Forgot Password ?</a>
                            </small>
                        </label>
                        <input type="password" id="password" class="ctrl" name="password" required="">
                    </div>
                    <div class="text-center text-danger">{{ "Incorrect Username or Password" if err}}</div>
                    <button class="btn success mt-4 ml-0 w-100" type="submit">Sign in</button>
                </form>
            </div>
        </div>
    </div>
    <img class="bg-img" src="/img/auth-bg.png" style="">
{% endblock %}

{% block style %}
    <style>
        .logo {
            max-height: 25px;
            margin: 1em auto;
            display: block;
        }
        .container {
            z-index: 100;
            margin-bottom: 14em;
        }
        body {
            display: grid;
            place-items: center;
        }
        .bg-img {
            max-width: 300px;
            position: absolute;
            right: 0;
            bottom: 10px;
            opacity: 0.6;
        }
    </style>
{% endblock %}